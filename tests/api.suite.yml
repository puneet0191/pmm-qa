actor: ApiTester
modules:
    enabled:
    - Asserts
    - REST:
        depends: PhpBrowser
        url: http://localhost:3000
    - SOAP:
        depends: PhpBrowser
        endpoint: api_url
        schema: wsdl